import React from 'react';
import './App.scss';
import { Counter } from './components/Counter';

const App: React.FC = () => {
  return (
    <section>
      <Counter />
    </section>

  );
};

export default App;
